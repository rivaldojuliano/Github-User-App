<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rivzdev.consumerapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.rivzdev.githubuserapp.READ_DATABASE"/>
    <uses-permission android:name="com.rivzdev.githubuserapp.WIRTEDATABASE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubUserApp">

        <activity android:name=".view.ui.activity.DetailUserActivity" />
        <activity android:name=".view.ui.activity.FavoriteActivity" />
        <activity android:name=".view.ui.activity.SettingActivity" />
        <activity android:name=".view.ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <queries>
        <package android:name="com.rivzdev.githubuserapp"/>
    </queries>

</manifest>